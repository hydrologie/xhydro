name: xhydro
channels:
  - conda-forge
dependencies:
  - python >=3.9,<3.12
  # Don't forget to sync changes between environment.yml, environment-dev.yml, and pyproject.toml!
  # Main packages
  - numpy
  - pooch >=1.8.0
  - pydantic >=2.0,<2.5.3
  - spotpy
  - statsmodels
  - xarray
  - xclim >=0.47.0
  - xscen >=0.7.1
  - pip
  - pip:
    - xdatasets
